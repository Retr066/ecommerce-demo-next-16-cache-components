import { RegisterForm } from '@/components/auth/register-form';

export const metadata = {
  title: 'Crear Cuenta',
  description: 'Crea tu cuenta para empezar a comprar',
};

export default function RegisterPage() {
  return <RegisterForm />;
}
